<div class="max-w-6xl m-auto">
  <%= link_to @genre_name, root_path %>
    <div class="flex flex-wrap items-center justify-between">
      <% @releases.each do |release| %>
        <article class="border rounded-lg pt-2 pb-2">
          <%= link_to image_tag(release.resized_photo), release %>
        </article>
      <% end %>
    </div>
</div>