var genres = document.getElementById("genres");
var releases = document.getElementById("releases");
window.history.replaceState(null, null, "?genre=<%= @genre_name %>?order=<%= @order%>");
genres.innerHTML = "<%= j render 'releases/genres' %>";